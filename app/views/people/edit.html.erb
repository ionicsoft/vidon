<% provide(:title, "Edit profile") %>
<div class="container">
  <%= render "flash_notice" %>
  <div class="row justify-content-center">
    <div class="col-5">
      <div class="card card-default">
        <h1 id="card-text" class="card-header">Edit</h1>
        <div class="card-body">
          <%= render 'form', person: @person %>
        </div>
      </div>
    </div>
  </div>
</div>
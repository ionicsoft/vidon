<% if logged_in? && current_person.customer? %>
  <div class="panel panel-default">
    <%= form_with(model: VideoComment.new, local: true, class: "panel-body") do |form| %>
    
      <%= form.hidden_field :video_id, :value => @video.id %>
      <%= form.hidden_field :customer_id, :value => current_person.user.id %>
    
      <div class="form-group">
        <%= form.label :comment, "Your comment:" %>
        <%= form.text_area :comment, id: :video_comment_comment, rows: "3", class: "form-control" %>
      </div>
    
      <div class="actions">
        <%= form.submit "Post comment", class: "btn btn-primary"%>
      </div>
    <% end %>
  </div>
<% end %>

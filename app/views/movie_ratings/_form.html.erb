<%= form_with(model: movie_rating, local: true) do |form| %>
  <% if movie_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_rating.errors.count, "error") %> prohibited this movie_rating from being saved:</h2>

      <ul>
      <% movie_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :movie_id %>
    <%= form.number_field :movie_id, id: :movie_rating_movie_id %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating, id: :movie_rating_rating %>
  </div>
  
  <div class="field">
    <%= form.label :customer_id %>
    <%= form.number_field :customer_id, id: :movie_rating_customer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

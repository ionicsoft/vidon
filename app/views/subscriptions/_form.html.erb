<%= form_with(model: subscription, local: true) do |form| %>
  <% if subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
      <% subscription.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.number_field :customer_id, id: :subscription_customer_id %>
  </div>

  <div class="field">
    <%= form.label :show_id %>
    <%= form.number_field :show_id, id: :subscription_show_id %>
  </div>

  <div class="field">
    <%= form.label :current_episode %>
    <%= form.number_field :current_episode, id: :subscription_current_episode %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

Hello @person.full_name,

Thank you for patronage. Here are the subscriptions that are expiring in 5 days:

<% @person.user.subscriptions.each do |sub| %>
<% sub.show.name %>

<% end %>

You have <% pluralize @person.user.open_slots, "show slot" %> available,
out of <% @person.user.slots %>.

Please confirm any changes that you would like to make on the site before the
month is over. Your renewal_date is <% @person.user.renewal_date.strftime("%A, %b %d") %>.

<%= form_with(model: show_actor, local: true) do |form| %>
  <% if show_actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show_actor.errors.count, "error") %> prohibited this show_actor from being saved:</h2>

      <ul>
      <% show_actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :show_actor_name %>
  </div>

  <div class="field">
    <%= form.label :show_id %>
    <%= form.number_field :show_id, id: :show_actor_show_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
